//Etapa 3 -  Testes de transição de estado (MeusEmprestimos.js)
//Simule a devolução e verifique se o estado da lista muda:

it('remove livro da lista após devolução bem-sucedida', async () => {
  // mock fetch e render, simule estado antes e após `handleDevolver`
});
import { render, fireEvent, waitFor } from '@testing-library/react-native';